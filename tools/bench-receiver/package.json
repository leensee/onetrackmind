{
  "name": "otm-bench-receiver",
  "version": "0.1.0",
  "private": true,
  "description": "LAN-only bench corpus receiver for the iOS capture-path spike. Structurally isolated from the production backend per the §10 bench-exception ruling (2026-07-21): no import path from backend/, binds a single LAN interface, refuses to start without a secret.",
  "type": "commonjs",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && tsx tests/pure.test.ts"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.1.0",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
